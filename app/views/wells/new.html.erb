<top-bar ng-init="init('Wells', 'New Well')"></top-bar>

<div class="container-fluid">
  <div class="row vert-margins">
    <%= render partial: 'client_info/navbar' %>
  </div>

  <%= render partial: 'form', locals: {well: @well, url: wells_url( @well, action: :create ) } %>
</div>